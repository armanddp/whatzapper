#!/usr/bin/env ruby

require 'whatzapper'

database = ARGV.pop
unless database
  puts "Usage: whatzapper </full_path_to_/ChatStorage.sqlite>"
else
  zapper = Whatzapper::Zapper.new database
  zapper.clean_db
end